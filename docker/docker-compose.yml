# Auto-generated by generate-compose.sh
# DO NOT EDIT MANUALLY - regenerate with: ./scripts/generate-compose.sh

services:
  validator-0:
    build:
      context: ..
      dockerfile: Dockerfile
    container_name: cipherbft-validator-0
    command: ["start", "--config", "/app/config/node.json"]
    volumes:
      - ./configs/node0:/app/config:ro
      - ../devnet/node0/keys:/app/keys:ro
      - ../devnet/node0/data:/app/data
      - ../devnet/genesis.json:/app/genesis.json:ro
    ports:
      - "8545:8545"
      - "8546:8546"
      - "19100:9100"
    networks:
      cipherbft:
        ipv4_address: 10.200.0.10
    environment:
      - RUST_LOG=info
    restart: unless-stopped
    #healthcheck:
    #  test: ["CMD", "curl", "-f", "http://localhost:8545"]
    #  interval: 10s
    #  timeout: 5s
    #  retries: 3
    #  start_period: 30s

  validator-1:
    build:
      context: ..
      dockerfile: Dockerfile
    container_name: cipherbft-validator-1
    command: ["start", "--config", "/app/config/node.json"]
    volumes:
      - ./configs/node1:/app/config:ro
      - ../devnet/node1/keys:/app/keys:ro
      - ../devnet/node1/data:/app/data
      - ../devnet/genesis.json:/app/genesis.json:ro
    ports:
      - "8555:8555"
      - "8556:8556"
      - "19101:9110"
    networks:
      cipherbft:
        ipv4_address: 10.200.0.11
    environment:
      - RUST_LOG=info
    restart: unless-stopped
    #healthcheck:
    #  test: ["CMD", "curl", "-f", "http://localhost:8545"]
    #  interval: 10s
    #  timeout: 5s
    #  retries: 3
    #  start_period: 30s

  validator-2:
    build:
      context: ..
      dockerfile: Dockerfile
    container_name: cipherbft-validator-2
    command: ["start", "--config", "/app/config/node.json"]
    volumes:
      - ./configs/node2:/app/config:ro
      - ../devnet/node2/keys:/app/keys:ro
      - ../devnet/node2/data:/app/data
      - ../devnet/genesis.json:/app/genesis.json:ro
    ports:
      - "8565:8565"
      - "8566:8566"
      - "19102:9120"
    networks:
      cipherbft:
        ipv4_address: 10.200.0.12
    environment:
      - RUST_LOG=info
    restart: unless-stopped
    #healthcheck:
    #  test: ["CMD", "curl", "-f", "http://localhost:8545"]
    #  interval: 10s
    #  timeout: 5s
    #  retries: 3
    #  start_period: 30s

  prometheus:
    image: prom/prometheus:v2.50.0
    container_name: cipherbft-prometheus
    volumes:
      - ./prometheus/prometheus.yml:/etc/prometheus/prometheus.yml:ro
    command:
      - '--config.file=/etc/prometheus/prometheus.yml'
      - '--storage.tsdb.path=/prometheus'
      - '--storage.tsdb.retention.time=7d'
    ports:
      - "19090:9090"
    networks:
      cipherbft:
        ipv4_address: 10.200.0.13
    restart: unless-stopped

  grafana:
    image: grafana/grafana:10.3.0
    container_name: cipherbft-grafana
    ports:
      - "13000:3000"
    environment:
      - GF_SECURITY_ADMIN_PASSWORD=admin
      - GF_AUTH_ANONYMOUS_ENABLED=true
    networks:
      cipherbft:
        ipv4_address: 10.200.0.14
    depends_on:
      - prometheus
    restart: unless-stopped

networks:
  cipherbft:
    driver: bridge
    ipam:
      config:
        - subnet: 10.200.0.0/24
          #gateway: 10.200.0.1

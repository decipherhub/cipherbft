[package]
name = "cipherbft-execution"
version = "0.1.0"
edition = "2021"
rust-version = "1.75"

[dependencies]
# Internal dependencies
cipherbft-types = { path = "../types" }
cipherbft-crypto = { path = "../crypto" }

# Reth primitives (note: reth-primitives crate doesn't exist on crates.io)
# Using alloy-consensus instead which provides compatible Ethereum types
# reth-primitives = "1.1"

# EVM execution
revm = { version = "19", default-features = false, features = ["std", "secp256k1"] }
revm-primitives = "19"

# Merkle trie for root computation (compatible with alloy 0.8)
alloy-trie = "0.7"

# Ethereum types (using Alloy v0.8 for c-kzg compatibility with revm 19)
alloy-primitives = "0.8"
alloy-consensus = { version = "0.8", features = ["serde"] }
alloy-eips = "0.8"
alloy-rlp = "0.3"

# Error handling
thiserror = "2"

# Derive macros (required by alloy dependencies)
derive_more = { version = "1.0", features = ["display", "from"] }

# Async runtime
tokio = { version = "1", features = ["full"] }
async-trait = "0.1"

# Concurrency
parking_lot = "0.12"

# Logging
tracing = "0.1"

# Serialization
serde = { version = "1", features = ["derive"] }

# Collections
lru = "0.12"
dashmap = "6"

# Encoding
hex = "0.4"

[dev-dependencies]
proptest = "1.4"
tempfile = "3.8"
criterion = { version = "0.5", features = ["html_reports"] }

[lints.rust]
unsafe_code = "deny"
